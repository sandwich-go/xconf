// Code generated by gotemplate. DO NOT EDIT.

package vars

import (
	"flag"
	"fmt"
	"reflect"
	"strings"
)

//template type Slice(KType,ParseKeyFunc,SetProviderByFieldType,StringValueDelim)

// KType 默认key类型

// SetProviderByFieldType 替换

// ParseKeyFunc val解析，替换

var typeNameSliceInt = ""

// StringValueDelim 数据分割符

func init() {
	v := []int{}
	typeNameSliceInt = fmt.Sprintf("[]%s", reflect.TypeOf(v).Elem().Name())
	SetProviderByFieldType(typeNameSliceInt, func(valPtr interface{}, stringAlias func(s string) string) flag.Getter {
		return NewSliceInt(valPtr.(*[]int), stringAlias)
	})
}

// Slice struct
type SliceInt struct {
	stringAlias func(s string) string
	s           *[]int
	set         bool // if there a flag defined via command line, the slice will be cleared first.
}

// NewSlice new func
func NewSliceInt(p *[]int, stringAlias func(s string) string) *SliceInt {
	return &SliceInt{
		stringAlias: stringAlias,
		s:           p,
		set:         false,
	}
}

// Set 解析时由FlagSet设定而来，进行解析
func (s *SliceInt) Set(str string) error {
	str = s.stringAlias(str)
	for _, v := range strings.Split(str, StringValueDelim) {
		got, err := parseInt(s.stringAlias(v))
		if err != nil {
			return err
		}
		if !s.set {
			*s.s = (*s.s)[:0]
			s.set = true
		}
		*s.s = append(*s.s, got)
	}
	return nil
}

// Get 返回数据
func (s *SliceInt) Get() interface{} {
	return []int(*s.s)
}

// TypeName type name for vars FlagValue provider
func (s *SliceInt) TypeName() string { return typeNameSliceInt }

// String 获取Set设置的字符串数据？或数据转换到的？
func (s *SliceInt) String() string {
	if s.s == nil {
		return ""
	}
	return fmt.Sprintf("%v", *s.s)
}

// Usage  usage info for FlagSet
func (s *SliceInt) Usage() string {
	return fmt.Sprintf("xconf/xflag/vars, value split by %s", StringValueDelim)
}
